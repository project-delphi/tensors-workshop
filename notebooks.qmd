---
title: "Notebooks"
subtitle: "One notebook per section, plus a take-home deep dive, each runnable cold in Colab"
---

Every section of the workshop has a notebook. One more sits past the end of the
day, as a take-home deep dive. They are **English-primary**, and every heading
carries a one-line Spanish summary — *cada encabezado lleva un resumen en
español.*

## How they are built

<span data-language-key="how-they-are-built"></span>

Every notebook has the same five parts, so you always know where you are:

1. **Header** — the section title, its objectives, and an *Open in Colab* badge.
2. **Setup** — installs and imports only what this section needs, and loads its
   own data. Each notebook is self-contained: no local files, and no dependency
   on any other notebook.
3. **Explanation and code**, alternating. They build up, rather than dropping a
   wall of code on you.
4. **Exercises** — `# TODO` stubs taken from the handbook, each followed by a
   folded solution cell. Open it after you have tried.
5. **Closing** — the Kahoot check for that section, and a link back here.

::: {.callout-important appearance="simple"}
The notebooks are committed with **no outputs and no execution counts**. Every
number you see is one you produced. The prose quotes the expected results, so
you can tell whether yours are right. If yours differ, that is worth
investigating rather than dismissing.
:::

## The notebooks

<span data-language-key="the-notebooks"></span>

{{< include _includes/notebooks-en.md >}}

## Going further

<span data-language-key="going-further"></span>

This one is **not a section**. It is a deep dive, handed out as take-home
material. It has no slides, no Kahoot, and no minutes on the workshop clock: the
{{< var workshop.minutes >}}-minute agenda is exactly what it was without it.

{{< include _includes/notebooks-extra-en.md >}}

## Running them somewhere other than Colab

<span data-language-key="running-them-somewhere-other-than-colab"></span>

The notebooks only need the scientific Python stack. Locally:

```bash
git clone {{< var repo.url >}}.git
cd {{< var repo.name >}}
uv run --group notebooks jupyter lab
```

The `notebooks` group is declared in `pyproject.toml`, and it is read off the
notebooks' own imports rather than written down beside them. `matplotlib` and
`ipywidgets` are in it because every notebook plots and all but 00 use sliders;
both ship with Colab, so a missing one only shows up when you run locally.

`scikit-learn` and `scikit-image` ship the tumour data, the handwritten digits
and the photographs. So notebooks 01, 03, 04, 06 and 13 run with no network at
all. The other nine download something the first time they run: a CSV from
GitHub, the storm video clip from Wikimedia, or the voice recording for
notebook 12's take-home E. The table below says which, notebook by notebook.

## What each notebook needs

<span data-language-key="what-each-notebook-needs"></span>

{{< include _includes/notebook-deps-en.md >}}

† Installed by the notebook itself with `%pip install -q`, which is why neither
is in the command above.
